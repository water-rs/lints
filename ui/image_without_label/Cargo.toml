[package]
name = "image_without_label"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
waterui = { version = "0.4.1", default-features = false, features = ["media", "gpu"] }

[[bin]]
name = "image_without_label"
path = "main.rs"

[workspace]
